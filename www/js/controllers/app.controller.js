define(['directives/app.directives'],function () {
   'use strict';
   var app = angular.module('app.controller', ['app.directives']);
   //展业首页控制器
   app.controller('indexController', function ($scope) {

   });
   return app;
});
